# ApiScope — Frontend (Dashboard de monitoreo)

Carpeta reservada para la **Fase 5** del proyecto (ver `../PROGRESS.md`): la
SPA embebida que sirve `/api/monitoring` (RF-03 de `../requerimientos-logger.md`).

Todavía no hay código acá — el backend (`../backend/`) va primero. Cuando
arranque la fase 5, este directorio tendrá su propio `package.json` y
proceso de build, empaquetado luego como un único HTML/CSS/JS embebido
dentro del paquete `apiscope` (< 500KB, sin build process para el consumidor
final).
